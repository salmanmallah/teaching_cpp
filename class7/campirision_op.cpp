#include <iostream>
using namespace std;

int main()
{
    // boolean;
    // true =  1
    // false = 0

    // comparision operator. (==, <, >, <=, >=, != ), !  not
    int x = 20;
    int y = 20;

    cout << (x >= y) << endl;
    cout << (x != y) << endl;
}